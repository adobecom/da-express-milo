When making a new kitchen sink document for a block, first create a new page with the 

an h1 header of the block's title, and a plain text subcopy describing the blocks purpose.

For now, just make a plain version of each block variant in sections below the header content

Use the template.html file as a guide for creating the content

Update the block and variant completeness report when done
